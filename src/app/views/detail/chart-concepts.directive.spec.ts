import { ChartConceptsDirective } from './chart-concepts.directive';

describe('ChartConceptsDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new ChartConceptsDirective();
    expect(directive).toBeTruthy();
  });
});
