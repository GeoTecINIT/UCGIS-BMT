import { ChartNotCommon2Directive } from './chart-not-common2.directive';

describe('ChartNotCommon2Directive', () => {
  it('should create an instance', () => {
    const directive = new ChartNotCommon2Directive();
    expect(directive).toBeTruthy();
  });
});
