import { ChartNotCommon1Directive } from './chart-not-common1.directive';

describe('ChartNotCommon1Directive', () => {
  it('should create an instance', () => {
    const directive = new ChartNotCommon1Directive();
    expect(directive).toBeTruthy();
  });
});
